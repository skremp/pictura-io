#
# Integration tests runtime properties
#

# Host and port number of the integration test server instance
it.host=${it.host}
it.port=${it.port}

# Root path to the native WebP libraries
it.libwebp=${it.libwebp}

# Load test
lt.report=${lt.report}
lt.response.dir=${lt.response.dir}